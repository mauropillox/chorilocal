# Example environment variables (do NOT commit secrets here)
# Copy this to .env locally and fill values.

DB_PATH=/data/ventas.db
SECRET_KEY=your-very-secret-key-here
DEBUG_MODE=false
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=https://www.pedidosfriosur.com,https://pedidosfriosur.com

# ============================================
# PRODUCTION TESTING (E2E Tests)
# ============================================
# Credentials for Playwright E2E tests against production
# Create a test user in production with READ-ONLY permissions
PROD_TEST_USER=test@example.com
PROD_TEST_PASSWORD=secure_password_here
PROD_URL=https://www.pedidosfriosur.com

SECRET_KEY=your-secure-random-key-change-this

# --- OPTIONAL ---
# Database path (default: /data/ventas.db)
DB_PATH=/data/ventas.db

# CORS origins (comma-separated list of allowed origins)
# Use * for development only!
CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:5173

# Environment (development or production)
ENVIRONMENT=development

# API key for backup health endpoint (optional)
# If set, backup health endpoint will require X-API-Key header
BACKUP_API_KEY=

# --- PRODUCTION ONLY ---
# External URL for keep-alive ping (Render/Heroku free tier)
# RENDER_EXTERNAL_URL=https://your-app.onrender.com
# SELF_URL=https://your-domain.com

# Render SSH credentials for backup scripts
# Get your service ID from Render dashboard
RENDER_SSH_USER=srv-your-service-id
RENDER_SSH_HOST=ssh.oregon.render.com
