[tool:pytest]
# CI runs only the minimal health test (comprehensive suite is local-only)
testpaths = .
python_files = test_ci_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
asyncio_default_fixture_loop_scope = function