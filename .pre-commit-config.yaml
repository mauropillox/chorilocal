repos:
  - repo: local
    hooks:
      - id: detect-secrets
        name: detect-secrets
        entry: bash scripts/secret-scan.sh
        language: system
        stages: [commit]
        files: ^(?!\.git).*